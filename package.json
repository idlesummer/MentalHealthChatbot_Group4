{
  "name": "cogni-monorepo",
  "private": true,
  "homepage": "https://github.com/rainev/cogni#readme",
  "repository": {
    "type": "git",
    "url": "https://github.com/rainev/cogni.git"
  },
  "scripts": {
    "build:cogni": "pnpm -C ../.. build",
    "build": "pnpm -r --if-present build",
    "lint": "pnpm -r --if-present lint",
    "test": "pnpm -r --if-present run test"
  },
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@10.28.2"
}
